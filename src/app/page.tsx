import { redirect } from "next/navigation";

export default function RootPage() {
  redirect("/board-list");
}

// BoardList is a collaborative workspace tool that is used by fast-moving teams to manage ideas, documentation, and execution. It is an internal SaaS product - a Collaborative Knowledge Board that lets you reorder cards within a column, move cards across columns, collaborate real-time with other users and communicate via comments.
